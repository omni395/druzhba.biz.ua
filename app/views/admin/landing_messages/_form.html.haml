%div{class: "list-group"}
  %div{class: "list-group-item"}
    = render Admin::FieldsetComponent.new(form, :name, :text, required: true)
  %div{class: "list-group-item"}
    = render Admin::FieldsetComponent.new(form, :phone, :text, required: true)
  %div{class: "list-group-item"}
    = render Admin::FieldsetComponent.new(form, :email, :email)
  %div{class: "list-group-item"}
    = render Admin::FieldsetComponent.new(form, :message, :text, required: true)
  %div{class: "list-group-item"}
    = render Admin::FieldsetComponent.new(form, :status, :radio, list: Admin::LandingMessage.statuses_i18n.invert)
