.list-group.mb-3
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :title, :text)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :image, :file)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :description, :text_area)
