.list-group.mb-3
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :title, :text, required: true)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :description, :text, required: true)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :flow, :radio, required: true, list: Admin::ExpenseCategory.flows_i18n.invert)
