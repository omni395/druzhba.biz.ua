model:
  validate:
    email: presence
    name: presence
    password: presence
  decorator:
  association:
  enum:
  active_storage:
app:
  title: Користувач системи
  index:
    conditions:
      - email:
          sign: eq
    list:
      fields:
        - email
        - name
      default_order:
    csv:
      fields:
  show:
    fields:
      - email
      - name
      - created_at
      - updated_at
  form:
    fields:
      - email:
          kind: email
      - name
      - password:
          kind: password

  association_search:
    conditions:
      - name:
          sign: full_like
    list:
        fields:
          - id
          - name
